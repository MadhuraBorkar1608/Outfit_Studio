<?xml version="1.0" encoding="utf-8"?>

<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:fillViewport="true">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="24dp">

        <!-- Screen Title -->
        <TextView
            android:id="@+id/tvAppearanceTitle"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Appearance Analysis"
            android:textSize="26sp"
            android:textStyle="bold"
            android:gravity="center"
            android:layout_marginBottom="24dp" />

        <!-- Image Preview -->
        <ImageView
            android:id="@+id/ivAppearancePreview"
            android:layout_width="match_parent"
            android:layout_height="280dp"
            android:layout_marginBottom="16dp"
            android:background="#E0E0E0"
            android:contentDescription="Selected appearance image"
            android:scaleType="centerCrop" />

        <!-- Select Image -->
        <Button
            android:id="@+id/btnSelectImage"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Select Image"
            android:layout_marginBottom="12dp" />

        <!-- Camera and Gallery -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:layout_marginBottom="12dp">

            <Button
                android:id="@+id/btnCamera"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:text="Camera"
                android:layout_marginEnd="6dp" />

            <Button
                android:id="@+id/btnGallery"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:text="Gallery"
                android:layout_marginStart="6dp" />

        </LinearLayout>

        <!-- Analyze Button -->
        <Button
            android:id="@+id/btnAnalyze"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Analyze"
            android:layout_marginBottom="16dp" />

        <!-- Loading Indicator -->
        <ProgressBar
            android:id="@+id/progressAppearance"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center"
            android:visibility="gone"
            android:layout_marginBottom="16dp" />

        <!-- Result Heading -->
        <TextView
            android:id="@+id/tvResultsTitle"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Analysis Results"
            android:textSize="20sp"
            android:textStyle="bold"
            android:layout_marginBottom="16dp" />

        <!-- Face Shape -->
        <TextView
            android:id="@+id/tvFaceShape"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Estimated Face Shape: --"
            android:textSize="17sp"
            android:layout_marginBottom="12dp" />

        <!-- Skin Tone -->
        <TextView
            android:id="@+id/tvSkinTone"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Estimated Skin Tone: --"
            android:textSize="17sp"
            android:layout_marginBottom="12dp" />

        <!-- Body Type -->
        <TextView
            android:id="@+id/tvBodyType"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Estimated Body Type: --"
            android:textSize="17sp"
            android:layout_marginBottom="12dp" />

    </LinearLayout>

</ScrollView>